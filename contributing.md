# contributing

## architecture

this repo is designed as a monorepo

## tasks

sync shared config and base files for subrepo packages:

```shell
moon run :sync-shared
```

sync shared base files for project root (`fuz`):

```shell
moon run sync-root
```
